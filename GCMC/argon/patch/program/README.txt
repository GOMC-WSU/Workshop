# To use the histogram method to generate phase diagram, we use the programs 
# in this directory. To compile and copy the executable file in correct 
# directory, run the "build.sh" script by following command:

chmod a+x build.sh

./build.sh

# Navigate your terminal to previous directory and follow the README.txt file.

cd ../.

